# قواعد وإرشادات العمل الخاصة بموقع مراجع وأدلة التطبيقات (App Review Engine)

## 1. آلية البحث عن التطبيقات والمرشحات (Candidate Search Flow)
- عندما يقوم المستخدم بالبحث عن اسم تطبيق غير موجود مسبقاً داخل قاعدة بيانات الموقع:
  1. يجب على الخادم/الموقع عدم إنشاء أو إضافة أية مراجعة عشوائية دون التأكد من وجود التطبيق في المتجر.
  2. يقوم النظام بالبحث في المتاجر الرسمية (Google Play / App Store) عن **جميع التطبيقات المطابقة أو المتقاربة لاسم البحث**.
  3. يتم عرض قائمة التطبيقات المرشحة (Candidate Apps) أسفل مربع البحث أو في نافذة اختيار منسقة ومفصلة ليقوم المستخدم باختيار التطبيق الذي يقصده بدقة متناهية.

## 2. توليد المراجعة والشرح التفصيلي (1500+ Word Review Standard)
- عند اختيار المستخدم للتطبيق المطلوب من نتائج البحث المقترحة:
  1. يتم استدعاء نموذج الذكاء الاصطناعي (OpenAI / Gemini) ببرومبت (Prompt) صريح يفرض كتابة **مقال مراجعة صحفي وشرح تفصيلي كامل لا يقل بحال من الأحوال عن 1500 كلمة (1500+ Words)**.
  2. يجب أن يشتمل المقال على الهيكل المنهجي المفصل:
     - # دليل ومراجعة شاملة لتطبيق [اسم التطبيق]
     - ## مقدمة استعراضية ورؤية التطبيق وفكرته الرئيسية
     - ## قصة وتاريخ المطور وأهداف تطوير التطبيق
     - ## الشرح الموسع والعميق لكافة المميزات والخصائص الفنية والوظائف الذكية
     - ## تحليل الأداء والسرعة، الأمان وحماية الخصوصية، واستهلاك الموارد
     - ## دليل الاستخدام والتشغيل الكامل خطوة بخطوة للمبتدئين
     - ## قسم الأسئلة الشائعة والأجوبة التفصيلية (FAQ)
     - ## العيوب والتحديات والملاحظات الموضوعية المصداقية
     - ## مقارنة شاملة مع التطبيقات المنافسة في المتاجر الرسمية
     - ## الخلاصة ورأي الخبراء والتقييم النهائي
     - ## الكلمات المفتاحية والدلالية المستهدفة (SEO Target Keywords) وتضم 15-25 كلمة مفتاحية دقيقة.

## 3. معايير الروابط المباشرة والموثوقية (SEO Direct Clean URLs)
- قاعدة الرابط الاحترافي المباشر (SEO URL Slug):
  * قم بتوليد معرف فريد (Slug) للتطبيق باللغة الإنجليزية مشتق بشكل مباشر وواضح من اسم التطبيق ليدل عليه بقوة في محركات البحث (مثال: WhatsApp -> wats أو whatsapp، و Facebook -> Facebook).
  * لا يُشترط عدد أحرف معين، ولكن يجب أن يكون معبراً واحترافياً.
  * إذا كان التطبيق نسخة معدلة أو مكررة، قم بإضافة تمييز بسيط في نهاية الرابط (مثال: Facebook Lite -> FacebookLite أو facel).
  * يمنع استخدام الرموز المعقدة أو المسافات، ويجب أن يكون الرابط مناسباً ليوضع مباشرة بعد الدومين هكذا: https://roohpro.com/Slug
- التأكد الدائم من صحة روابط الانتقال الخاصة بالمتجر (Google Play / App Store) للتطبيق المطلوب والتثبت من توفره الفعلي قبل إضافته أو توجيه المستخدم إليه.

## 4. نظام التخزين الخفيف واستنزاف الكوتا (Cloudflare R2 & approved-apps.json Architecture)
- **فصل المحتوى عن Firebase نهائياً**:
  1. عند توليد محتوى المقال الشامل (1500+ كلمة)، يُحفظ المحتوى كاملاً كملف `.html` في Cloudflare R2 فقط على المسار: `https://roohpro.com/clean-slug.html`.
  2. **يُمنع منعاً باتاً** حفظ نص المقال الطويل (1500+ كلمة) داخل قاعدة بيانات Firebase (Firestore) لتوفير الحصة والحد من استنزاف الكوتا.
- **تسجيل الروابط المعتمدة كقائمة ثابتة خفيفة (approved-apps.json)**:
  1. يتم حفظ ملف مركزي خفيف جداً باسم `approved-apps.json` على Cloudflare R2 ويتم تحديثه عند موافقة المدير على نشر أي تطبيق.
  2. واجهة الموقع للزوار (Frontend) تقرأ قائمة التطبيقات والمقالات المعتمدة مباشرة من `approved-apps.json` دون الاتصال بـ Firebase، مما يضمن عمل الموقع بنسبة 100% للزوار حتى لو توقف Firebase بسبب استنفاذ الباقة المجانية.
- **صيغة الروابط والأرشفة الديناميكية (Dynamic Sitemap)**:
  1. رابط المقال النظيف: `https://roohpro.com/clean-slug` (ممنوع إضافة .html في روابط الموقع الخارجية والـ Sitemap).
  2. خريطة الموقع `sitemap.xml`: تولد برمجياً وديناميكياً بالقراءة المباشرة من `approved-apps.json`.
  3. عند دخول الزائر للرابط `https://roohpro.com/clean-slug`، يجلب الموقع محتوى الـ HTML مباشرة من R2 (`clean-slug.html`) ويعرضه للزائر دون استهلاك أي قراءات من Firebase.

